<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".user.customer.addvendor.AddVendorActivity"></activity>

        <activity android:name=".user.vendor.VendorActivity" />
        <activity android:name=".user.customer.CustomerActivity" />
        <activity android:name=".user.vendor.addcustomer.AddCustomerActivity" />
        <activity
            android:name=".user.UserSelectionActivity"
            android:theme="@style/AppTheme"
            android:uiOptions="splitActionBarWhenNarrow" />
        <activity
            android:name=".register.RegisterActivity"
            android:enabled="true"
            android:label="@string/title_activity_register"
            android:launchMode="singleTask"
            android:parentActivityName=".AppMainActivity" />
        <activity
            android:name=".login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".AppMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>